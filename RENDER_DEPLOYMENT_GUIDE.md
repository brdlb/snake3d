# Пошаговая инструкция: Деплой на Render.com

## 📋 Что вам понадобится

- Аккаунт на [GitHub](https://github.com) (ваш код должен быть в репозитории)
- Аккаунт на [Render.com](https://render.com) (бесплатная регистрация)
- Аккаунт на [Cloudflare](https://dash.cloudflare.com) (для фронтенда)

## 🚀 Часть 1: Деплой Socket.IO сервера на Render

### Шаг 1: Подготовка кода

1. **Убедитесь, что все изменения закоммичены:**
   ```bash
   git add .
   git commit -m "Add Render deployment configuration"
   git push
   ```

2. **Проверьте, что файл `render.yaml` существует** в корне проекта (уже создан ✅)

### Шаг 2: Создание Web Service на Render

1. **Зайдите на [render.com](https://render.com)** и войдите в аккаунт

2. **Нажмите "New +" → "Web Service"**

3. **Подключите GitHub репозиторий:**
   - Если это первый раз, нажмите "Connect GitHub"
   - Авторизуйте Render для доступа к вашим репозиториям
   - Выберите репозиторий `snake3d`

4. **Render автоматически обнаружит `render.yaml`:**
   - Вы увидите сообщение: "Blueprint detected"
   - Нажмите "Apply" или "Create Web Service"

5. **Проверьте настройки:**
   - **Name:** `snake3d-server` (или любое другое имя)
   - **Region:** Выберите ближайший к вам регион:
     - `Oregon (US West)` - для Америки/Азии
     - `Frankfurt (EU Central)` - для Европы
     - `Singapore (Southeast Asia)` - для Азии
   - **Branch:** `main` (или `master`)
   - **Build Command:** `npm install && npm run server:build`
   - **Start Command:** `npm run start`
   - **Plan:** `Free`

6. **Environment Variables** (переменные окружения):
   - `NODE_ENV` = `production` (уже установлено в render.yaml)
   - `PORT` - оставьте пустым, Render установит автоматически
   - `FRONTEND_URL` - пока оставьте как есть, обновим позже

7. **Нажмите "Create Web Service"**

### Шаг 3: Ожидание деплоя

1. Render начнет сборку и деплой:
   ```
   ==> Cloning from GitHub...
   ==> Installing dependencies...
   ==> Building server...
   ==> Starting server...
   ==> Your service is live 🎉
   ```

2. **Процесс займет 3-5 минут**. Вы можете наблюдать за логами в реальном времени.

3. **После успешного деплоя** вы увидите:
   - ✅ Зеленый статус "Live"
   - URL вашего сервера, например: `https://snake3d-server.onrender.com`

4. **Скопируйте URL сервера** - он понадобится для следующего шага!

### Шаг 4: Проверка работы сервера

1. **Откройте URL сервера в браузере:**
   ```
   https://snake3d-server.onrender.com/health
   ```

2. **Вы должны увидеть JSON ответ:**
   ```json
   {
     "status": "ok",
     "timestamp": "2025-12-12T14:00:00.000Z",
     "uptime": 123.45,
     "connections": 0
   }
   ```

3. **Если видите это - сервер работает! ✅**

---

## 🌐 Часть 2: Деплой фронтенда на Cloudflare Pages

### Шаг 1: Создание проекта на Cloudflare Pages

1. **Зайдите на [dash.cloudflare.com](https://dash.cloudflare.com)**

2. **Выберите "Workers & Pages" в левом меню**

3. **Нажмите "Create application" → "Pages" → "Connect to Git"**

4. **Подключите GitHub:**
   - Авторизуйте Cloudflare
   - Выберите репозиторий `snake3d`

5. **Настройте build:**
   - **Project name:** `snake3d` (или любое другое)
   - **Production branch:** `main` (или `master`)
   - **Build command:** `npm run build`
   - **Build output directory:** `dist`
   - **Root directory:** оставьте пустым

### Шаг 2: Настройка переменных окружения

⚠️ **ВАЖНО:** Это критически важный шаг!

1. **Раскройте секцию "Environment variables"**

2. **Добавьте переменную:**
   - **Variable name:** `VITE_SOCKET_SERVER_URL`
   - **Value:** URL вашего Render сервера (из Части 1, Шаг 3)
     - Например: `https://snake3d-server.onrender.com`
   - **Environment:** `Production` (можно также добавить для `Preview`)

3. **Нажмите "Save and Deploy"**

### Шаг 3: Ожидание деплоя

1. Cloudflare начнет сборку:
   ```
   Initializing build environment...
   Cloning repository...
   Installing dependencies...
   Building application...
   Deploying to Cloudflare's global network...
   Success! 🎉
   ```

2. **Процесс займет 2-3 минуты**

3. **После успешного деплоя** вы получите URL:
   - Например: `https://snake3d.pages.dev`
   - Или: `https://abc123.snake3d.pages.dev` (для preview)

### Шаг 4: Обновление CORS на сервере

Теперь нужно разрешить фронтенду подключаться к серверу:

1. **Вернитесь в Render Dashboard**

2. **Откройте ваш Web Service (`snake3d-server`)**

3. **Перейдите в "Environment"**

4. **Обновите переменную `FRONTEND_URL`:**
   - Измените значение на: `https://snake3d.pages.dev` (ваш реальный URL)
   - Нажмите "Save Changes"

5. **Render автоматически перезапустит сервер** (займет ~30 секунд)

---

## ✅ Часть 3: Проверка работы

### Шаг 1: Откройте ваше приложение

1. Перейдите по URL: `https://snake3d.pages.dev`

2. **Откройте DevTools (F12) → Console**

3. **Вы должны увидеть:**
   ```
   [Network] Connecting to https://snake3d-server.onrender.com...
   [Network] Connected to server
   [Network] Authenticated as SwiftSnake123
   ```

4. **Если видите эти сообщения - всё работает! 🎉**

### Шаг 2: Проверка игры

1. Попробуйте начать игру
2. Проверьте, что данные сохраняются (обновите страницу - ваш username должен остаться)
3. Проверьте статистику и настройки

---

## 🔧 Troubleshooting (Решение проблем)

### ❌ Проблема: "Failed to connect to server"

**Возможные причины:**

1. **Сервер "заснул" (Free tier Render):**
   - Бесплатный tier Render засыпает после 15 минут неактивности
   - Первое подключение может занять 30-60 секунд (сервер просыпается)
   - **Решение:** Просто подождите ~1 минуту и обновите страницу

2. **Неправильный URL сервера:**
   - Проверьте `VITE_SOCKET_SERVER_URL` в Cloudflare Pages
   - Убедитесь, что URL начинается с `https://` (не `http://`)
   - **Решение:** Исправьте URL и пересоберите проект

3. **Сервер не запустился:**
   - Откройте Render Dashboard → Logs
   - Проверьте наличие ошибок
   - **Решение:** Исправьте ошибки и сделайте redeploy

### ❌ Проблема: CORS ошибка

**Симптомы:**
```
Access to XMLHttpRequest has been blocked by CORS policy
```

**Решение:**

1. Проверьте, что `FRONTEND_URL` на Render установлен правильно
2. Убедитесь, что URL точно совпадает (без trailing slash `/`)
3. Проверьте логи сервера - должно быть сообщение о разрешенных origins

### ❌ Проблема: Build failed на Cloudflare

**Возможные причины:**

1. **Не установлена переменная `VITE_SOCKET_SERVER_URL`:**
   - Добавьте её в Environment variables
   - Нажмите "Retry deployment"

2. **TypeScript ошибки:**
   - Проверьте логи сборки
   - Исправьте ошибки локально: `npm run build`
   - Закоммитьте и запушьте исправления

### ❌ Проблема: Build failed на Render

**Возможные причины:**

1. **Отсутствуют зависимости:**
   - Проверьте, что все зависимости в `package.json`
   - Локально запустите: `npm run server:build`

2. **TypeScript ошибки в серверном коде:**
   - Проверьте логи сборки
   - Исправьте ошибки локально
   - Закоммитьте и запушьте

---

## 📊 Мониторинг и логи

### Render Logs

1. Откройте Render Dashboard → ваш Web Service
2. Перейдите в "Logs"
3. Здесь вы увидите все логи сервера в реальном времени

**Полезные логи:**
```
[Auth] Data directory ready: /opt/render/project/src/data/users
[Connection] New client connected: abc123
[Auth] Login attempt with token: 12345678...
[Network] Connected to server
```

### Cloudflare Pages Logs

1. Откройте Cloudflare Dashboard → Workers & Pages → ваш проект
2. Перейдите в "Deployments"
3. Нажмите на конкретный deployment для просмотра логов сборки

---

## 🎯 Полезные команды

### Локальное тестирование перед деплоем

```bash
# Тестирование сборки сервера
npm run server:build
npm run start

# Тестирование сборки фронтенда
npm run build
npm run preview
```

### Обновление после изменений

```bash
# 1. Закоммитьте изменения
git add .
git commit -m "Your changes"
git push

# 2. Render и Cloudflare автоматически пересоберут проект
# (если включен auto-deploy)
```

### Ручной redeploy

**Render:**
1. Dashboard → ваш Web Service → "Manual Deploy" → "Deploy latest commit"

**Cloudflare Pages:**
1. Dashboard → ваш проект → "Deployments" → "Retry deployment"

---

## 💡 Советы

### 1. Ускорение "пробуждения" Render

Бесплатный tier Render засыпает после 15 минут. Чтобы сервер был всегда доступен:

**Вариант A:** Используйте внешний сервис для ping (например, UptimeRobot):
- Создайте monitor на `https://snake3d-server.onrender.com/health`
- Интервал: каждые 5 минут
- Сервер не будет засыпать

**Вариант B:** Апгрейд на платный план Render ($7/месяц):
- Сервер не засыпает
- Больше ресурсов
- Приоритетная поддержка

### 2. Custom Domain

**Для фронтенда (Cloudflare Pages):**
1. Dashboard → ваш проект → "Custom domains"
2. Добавьте ваш домен (например, `snake3d.example.com`)
3. Cloudflare автоматически настроит SSL

**Для сервера (Render):**
1. Dashboard → ваш Web Service → "Settings" → "Custom Domain"
2. Добавьте поддомен (например, `api.snake3d.example.com`)
3. Настройте CNAME запись в DNS

⚠️ **Не забудьте обновить:**
- `VITE_SOCKET_SERVER_URL` на Cloudflare Pages
- `FRONTEND_URL` на Render

### 3. Мониторинг использования

**Render Free Tier лимиты:**
- 750 часов в месяц (достаточно для одного сервиса 24/7)
- 100 GB bandwidth
- Сервер засыпает после 15 минут неактивности

**Cloudflare Pages лимиты:**
- Unlimited requests
- Unlimited bandwidth
- 500 builds в месяц

---

## 🎉 Готово!

Теперь ваше приложение развернуто и доступно по всему миру!

- **Фронтенд:** `https://snake3d.pages.dev`
- **Сервер:** `https://snake3d-server.onrender.com`

Поделитесь ссылкой с друзьями и наслаждайтесь игрой! 🐍
